name: skibidi_roast
description: "SkibidiRoast 😎🔥 is a fun app where users upload images 📸 and get instant, AI-generated roasts 🤖💥. It’s a fun, interactive way to see witty, playful burns 😏🔥 tailored to your photos, giving users a hilarious 😂 and shareable 🤳 experience."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.0.0
  smooth_page_indicator: ^1.2.1
  shared_preferences: ^2.5.3
  image_picker: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
